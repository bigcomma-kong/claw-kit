# Project: <프로젝트 이름>

<한 줄 설명 — 예: 사내 포털 백엔드 + 관리자 콘솔>

> 이 파일은 **시작용 템플릿**입니다. `<...>` 자리를 실제 값으로 채운 뒤
> `<project-name>.md` 로 저장하세요. **실제 파일은 커밋 금지** (.gitignore 처리됨).

## 기본 정보
- **프로젝트 경로**: `<C:\path\to\project 또는 /path/to/project>`
- **스택**: `<예: Next.js 15 + TypeScript + PostgreSQL>`
- **운영 서버**: `<있으면 기재 — 공개 저장소엔 넣지 말 것>`
- **배포 방식**: `<수동 / CI 자동 등>`

## 주요 모듈
- **<모듈명>** (`<경로>`): <설명>
- **<모듈명>** (`<경로>`): <설명>

## 디렉토리 구조
```
<src 기준 간단 트리>
```

## 코딩 컨벤션
- <언어·버전 — 예: TypeScript strict, Node 20>
- <스타일 — 예: 함수 50줄 이하, 파일 400줄 권장>
- <불변성·에러 처리 규칙 등>

## 금지/주의
- <예: 운영 DB 쓰기 금지 (SELECT만)>
- <예: 승인 없이 커밋·배포 금지>
- <예: 특정 레거시 디렉토리 수정 금지>

## 알려진 이슈
- <P0/P1 이슈를 적어두면 매번 재발견하지 않아도 됨>

## 외부 리소스
- <문서·대시보드·로그 경로 등 — 민감 정보 제외>

## Git
- 메인 브랜치: `<main / master>`
- 커밋 규칙: `<예: conventional commits — feat/fix/refactor>`
